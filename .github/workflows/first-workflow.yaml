name: fist worlflow
on:
  push:
    branches:
      - "main"
jobs:
  first_workflow:
    name: first_workflow
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - name: clean package
        run: mvn clean package
